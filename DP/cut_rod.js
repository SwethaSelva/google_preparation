/**
 * Given the length of rod n and cost for cut size array, find the optimize cut to max the profit.
 * Eg: n = 7, prices per cut size = [1,3,4,5,7,9,10,11]
 */

function maxPriceonCut(n, prices) {
  let result = 0;
  function backTracking (rem, curProfit = 0) {
    if (rem >= n) return result;

    for (let i = 0; i < )
  }
  return result;
}